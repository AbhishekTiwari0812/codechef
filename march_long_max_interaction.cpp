#include <iostream>
#include <algorithm>
#define rep(i,n) for((int)(i)=0;(i)<(n);i++)
#define all(v) (v.begin(),v.end())
using namespace std;

int main(){
	int t;
	while(t--){
		int n,k;
		int *A=new int[n];
		int *B=new int[n];
		rep(i,n)
			cin>>A[i];
		rep(i,n)
			cin>>B[i];
	}
	return 0;
}